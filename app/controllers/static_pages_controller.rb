# A controller to handle rendering a static page to serve as my homepage.
class StaticPagesController < ApplicationController

  # Define the root function to implicitly return "render :root"
  def root
  end
  
end
