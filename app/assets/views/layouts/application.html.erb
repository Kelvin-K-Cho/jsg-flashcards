<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>JSG-Flashcards</title>
    <% if current_user %>
      <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>",
          "image_url": "<%= asset_url current_user.image.url %>"
        };
      </script>
    <% end%>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
